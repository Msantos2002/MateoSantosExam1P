@model IEnumerable<MateoSantosExam1P.Models.MS_Cerveza>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MS_CervezaName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MS_CervezaDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MS_Escarchada)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MS_Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MS_Date)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MS_CervezaName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MS_CervezaDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MS_Escarchada)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MS_Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MS_Date)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MS_CervezaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MS_CervezaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MS_CervezaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
